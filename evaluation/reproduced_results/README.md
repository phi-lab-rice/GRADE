# Newly reproduced paper artifacts

`python evaluation/reproduce_paper.py` writes regenerated tables, figures, and
`comparison.html` here. It uses fresh `../metric_results/merged_csv/` inputs
by default and places each reproduced table/figure beside the golden equivalent
in the HTML report.
